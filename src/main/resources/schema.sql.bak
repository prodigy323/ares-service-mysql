DROP TABLE IF EXISTS HERO;

CREATE TABLE hero (
    id INTEGER NOT NULL AUTO_INCREMENT,
    firstname VARCHAR(32),
    lastname VARCHAR(32),
    codename VARCHAR(32),
    email VARCHAR(32),
    team VARCHAR(32),
    PRIMARY KEY (id)
);